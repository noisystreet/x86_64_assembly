.. _chapter-07-calling-c:

===============================
从汇编调用 C 函数
===============================

.. 本篇内容

    - 声明外部符号
    - 传递参数与获取返回值
    - 调用 printf 等标准库函数
    - 链接时注意事项
    - g++ 与名称修饰（扩展话题）

.. note::

   本书主线使用 **GCC (C)** 讲解。如果使用 **g++（C++）**，由于名称修饰（name mangling），
   汇编中引用的函数名会被重整，需用 ``extern "C"`` 来兼容。
   详见本节末尾的扩展话题。

TODO: 编写内容
