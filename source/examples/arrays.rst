.. _example-arrays:

==========================
数组操作演示
==========================

.. code-block:: none

   ; 编译：nasm -f elf64 arrays.asm -o arrays.o && ld arrays.o -o arrays
   ; 运行：./arrays

.. literalinclude:: arrays.asm
   :language: none
   :linenos:
