.. _example-strings:

==========================
字符串操作演示
==========================

.. code-block:: none

   ; 编译：nasm -f elf64 strings.asm -o strings.o && ld strings.o -o strings
   ; 运行：./strings

.. literalinclude:: strings.asm
   :language: none
   :linenos:
